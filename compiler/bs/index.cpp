#include "translator.cpp"

#include <fstream>
#include <iostream>

int main() {
    while (true) {
        std::string command;
        std::getline(std::cin >> std::ws, command);


        if (command=="exit") break;

        if (command=="comp")
        
    }

    return 0;
}
